import Button from "../commponents/Button";
import Card from "../commponents/Card";
const Frame2 = ({ onSet }) => {
  return (
    <div>
      <Button />
      <Card onSet={onSet} />
    </div>
  );
};
export default Frame2;
